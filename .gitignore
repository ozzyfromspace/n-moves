# Dependencies
node_modules

# Nuxt / Nitro build output
.nuxt
.nitro
.cache
.output
.data
dist

# Vercel
.vercel

# Logs
*.log
logs

# Env
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
*.tsbuildinfo

# Built positions dataset — large, regenerated by scripts/build-positions.ts.
# Only the committed sample (positions.sample.json) is tracked in git.
public/positions/positions.json
