# Dependencies
node_modules

# Nuxt / Nitro build output
.nuxt
.nitro
.cache
.output
.data
dist

# Vercel
.vercel

# Logs
*.log
logs

# Env
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
*.tsbuildinfo

# The built positions dataset ships committed with the app (alongside the sample).
# Regenerate it with scripts/build-positions.ts (see its header) or the "Build
# positions" Action, which opens a PR with a fresh set rather than committing here.

# Raw Lichess dump prefixes fetched while (re)building positions — never commit
# these (hundreds of MB); the builder reads them and the output is positions.json.
/eval.zst
/eval.jsonl
