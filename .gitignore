# Dependencies
node_modules

# Nuxt / Nitro build output
.nuxt
.nitro
.cache
.output
.data
dist

# Vercel
.vercel

# Logs
*.log
logs

# Env
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
*.tsbuildinfo

# The built positions dataset ships with the app — the offline build script
# (scripts/build-positions.ts) isn't run in CI, so positions.json is committed
# alongside the sample. Regenerate it with that script (see its header).
